interface:
  display_name: "Resolve GitHub PR"
  short_description: "Resolve PR review comments with gh"
  default_prompt: "Use $github-resolve-pr to fix the current branch PR review comments and mark resolved threads."
